## Dataset

This project uses a satellite land classification dataset provided via IBM Skills Network Labs.

The dataset is downloaded automatically inside the notebooks using a remote URL and is not included in this repository.

To reproduce results:
- Run the notebooks from the `notebooks/` directory
- The dataset will be downloaded and extracted automatically
